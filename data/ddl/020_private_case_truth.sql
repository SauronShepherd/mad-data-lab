CREATE TABLE IF NOT EXISTS {{PRIVATE}}.case_truth (case_id STRING, primary_component STRING, primary_source STRING, primary_cause STRING, secondary_cause STRING, affected_rows INT, expected_impact DECIMAL(18,2), secondary_expected_impact DECIMAL(18,2), expected_total_deviation DECIMAL(18,2), confidence STRING, allowed_final_status_json STRING, expected_path_json STRING, truth_json STRING) USING DELTA
