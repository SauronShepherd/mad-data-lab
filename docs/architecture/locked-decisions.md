# Locked architecture decisions

- Browser receives only public/curated evidence; private truth is deployment-only.
- Case #042 canonical content is generated by `data/generation`, not frontend constants.
- Trusted SQL uses a closed query registry and native positional parameters.
- Secondary Cases remain unavailable until their own deterministic contracts exist.
- Live Databricks evidence remains an evidence gate; artwork is implementation-owned and never blocks the game submission.
