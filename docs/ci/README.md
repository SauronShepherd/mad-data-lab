# CI evidence directory

Required-check definitions are in [required-checks.md](required-checks.md). Run reports are emitted under `release-report/`; external GitHub status evidence is intentionally not fabricated locally.
