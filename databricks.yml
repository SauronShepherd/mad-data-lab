bundle:
  name: mad-data-lab-mdl2

include:
  - resources/*.yml

targets:
  staging:
    mode: development
    workspace:
      profile: mdl
      root_path: ~/.bundle/${bundle.name}/${bundle.target}
